import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register",
  description: "Register your team for IN-SPACe Model Rocketry / CANSAT India Student Competition 2026-27. Open to school and college students across India.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
